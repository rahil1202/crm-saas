# Tasks

- [x] 1. Optimize database connection pool and create worker pool
  - [x] 1.1. Increase main pool `max` from 10 to 40 in `backend/src/db/client.ts`
  - [x] 1.2. Remove `prepare: false` to enable prepared statements
  - [x] 1.3. Add `idle_timeout: 20` and `connect_timeout: 10` options
  - [x] 1.4. Create a separate `workerClient` with `max: 8` for background tasks
  - [x] 1.5. Export `workerDb` drizzle instance and `workerPgClient`
- [x] 2. Remove profile upsert from auth middleware
  - [x] 2.1. Remove the `db.insert(profiles).values(...).onConflictDoUpdate(...)` block from `requireAuth` in `backend/src/middleware/auth.ts`
  - [x] 2.2. Keep token verification, session check, and super admin check intact
- [x] 3. Replace DB-based rate limiting with in-memory implementation
  - [x] 3.1. Create in-memory Map-based sliding window counter in `backend/src/lib/security.ts`
  - [x] 3.2. Replace `consumeRateLimit` function body with in-memory logic keeping same signature
  - [x] 3.3. Add periodic cleanup interval (every 60s) with `.unref()` to evict expired windows
  - [x] 3.4. Keep same error throwing behavior (AppError.tooManyRequests with same details)
- [x] 4. Update automation runtime to use worker database pool
  - [x] 4.1. Import `workerDb` from `@/db/client` in `backend/src/lib/automation-runtime.ts`
  - [x] 4.2. Replace `db` with `workerDb` for worker-tick functions: `claimDueAutomationRuns`, `processAutomationRun`, `upsertRunStep`, `markRunFailure`, `isRunCanceled`, `queueLeadInactiveTriggers`
  - [x] 4.3. Keep `db` for user-facing route functions: `listAutomationRuns`, `getAutomationRunDetail`, `cancelAutomationRun`, `enqueueAutomationRun`, `recordTriggerEvent`
- [x] 5. Add response compression middleware
  - [x] 5.1. Import `compress` from `hono/compress` in `backend/src/app/route.ts`
  - [x] 5.2. Add `app.use("*", compress())` after the `applySecurityHeaders` middleware
- [x] 6. Optimize sequence runtime with batch queries
  - [x] 6.1. In `processDueSequenceRuns` in `backend/src/lib/sequence-runtime.ts`, collect all unique (companyId, sequenceId, stepIndex) tuples from due enrollments
  - [x] 6.2. Batch-fetch all needed current steps AND next steps in a single query
  - [x] 6.3. Build a Map lookup keyed by `${companyId}:${sequenceId}:${stepIndex}`
  - [x] 6.4. Replace individual step queries in the loop with Map lookups
